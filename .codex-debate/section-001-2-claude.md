**claude** — Fixed the notification-gesture, badge-rejection, stale-pip, set-equality, and two stale-comment findings; typecheck green and the 11 alerts/metrics tests pass.

- `F1` **partial** — Real on gesture-requiring browsers: Firefox and Safari suppress a bare init-time `Notification.requestPermission()` (no prompt) unless it rides user activation, so the eager `void notify.requestPermission()` at `App.tsx:557` left first-time users on those browsers silently un-permissioned. Fixed the actual defect (the timing): the request now fires from a one-shot `pointerdown`/`keydown` listener on `window` that removes itself + its sibling and is torn down via `onCleanup`, so exactly one prompt is raised, inside a gesture, and nothing leaks past teardown (App.tsx ~557). `requestPermission` stays idempotent (immediate no-prompt resolve once granted/denied) and a no-op on unsupported browsers. DEFERRED the second half of codex's suggestion — an explicit "Enable notifications" control that surfaces denied/unsupported status — as product-UI scope beyond this bridge PR; the enablement defect itself is closed.
- `F2` **fixed** — Correct: `void nav.setAppBadge?.(count)` discards the value, not a rejection, so a Badging-API reject (permission/security/document-state) became an unhandled rejection each time the effect ran. Now routed through `badgeOp?.catch(...)` with a warn (App.tsx ~587), preserving the unsupported-API `?.` no-op.
- `F3` **fixed** — Confirmed: the alert pip (`App.tsx:805`) sits in the card HEADER, OUTSIDE the `<Show when={entryState().kind === "connected"}>` gate that wraps the metrics body — so a disconnected host kept painting its stale last-known set as a live red "N alerts" pip, while the app badge (`w?.kind === "live"`) correctly excluded it. Gated the pip on `entryState().kind === "connected" && alertCount() > 0`, matching the badge's live-only policy and the metrics body's own disconnect gate; comment updated at ~757.
- `F4` **fixed** — Conceded as a cheap hardening. The producer (`applyHysteresis`, canonicalizing via `METRIC_IDS.filter`) never emits a repeat, so no real value triggered the gap — but the exported `alertsEqual` claims set equality and the length+one-way-membership form did mistake `["cpu","cpu"]` for `["cpu","mem"]`. Rewrote it as true set equality (`Set` size + membership) at alerts.ts:106; not a fallback, just honest to its own docstring.
- `F5` **fixed** — Correct on both counts. Verified in the pinned reactor: `scan` (`reactor.ts:242`) calls `src.subscribe(...)` SYNCHRONOUSLY at construction, which runs `source`'s `install` (`reactor.ts:142`) and sets `emitMetrics`. `scan(metrics, …)` is constructed while `implementSurface`'s `cells` literal is evaluated — BEFORE the runtime fires the `derived.cell` connect effect — so the tap installs at construction, not via connect, and goes null only after scan/source teardown at dispose. Rewrote both misleading comments (main.ts ~193 and the tick comment ~317) to say so.

Environment note: codex's "full suite not clean" (EROFS on temp-dir tests, one unchanged process-stream failure) is a read-only-sandbox artifact, not a code defect — the targeted alerts/metrics tests and full typecheck are green here.

commit: `e725743e1f94caf29f13b260bd973ef02945004b`
