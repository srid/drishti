DELIBERATE decisions codex must NOT flag: (1) the alerts scan passes NO store — a fresh process re-derives its level from fresh samples (must NOT survive restart). (2) applyHysteresis returns the PREV state reference on no-change so scan suppresses the publish (identity return intentional). (3) the alerts contract cell is reactor-free (schema/default/verbs/equals) so the agent-shared graph stays pure — reactor primitives are used ONLY in agent/main.ts (agent-boots guards this). (4) the badge = count of hosts with ANY live alert (drishti's own fold, NOT a sum; the two consumers deliberately disagree — kolu sums awaiting terminals). (5) item ids are the metric-series names cpu/mem/disk (stable, = MetricKey). (6) the parent re-serve alertsStore is drishti's own mirror pump, separate from @kolu/surface-remote. (7) THE surface.md drishti gate is already satisfied — this repo IS the drishti PR, pinned to kolu's final HEAD; do not raise it as a blocking finding. (8) alerts is drishti's OWN cell — kolu does not consume it, so the wire-shape reshape is internal to drishti.
